# These are the options executed by the current release. Other typed settings
# remain reserved for future engines and are rejected when changed from defaults.
pdf:
  fallback_render_dpi: 250

docling:
  enabled: true
  ocr: false
  table_structure: true
  formula_enrichment: true
  picture_images: true
  code_enrichment: true
  force_backend_text: true
